version: '3.1'

services:
  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: one_piece
    volumes:
      - ./modulo_2/SQL/ddl.sql:/docker-entrypoint-initdb.d/0ddl.sql
      - ./modulo_2/SQL/dml.sql:/docker-entrypoint-initdb.d/2dml.sql
      - ./modulo_3/triggger_stored_procedure.sql:/docker-entrypoint-initdb.d/1triggger_stored_procedure.sql

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: postgres@email.com
      PGADMIN_DEFAULT_PASSWORD: postgres
      PGADMIN_LISTEN_ADDRESS: 0.0.0.0
    depends_on:
      - db
    ports:
      - 8080:80
